<!-- Solid field behind the adaptive icon. #2E4034 is the background of the
     VTB icon, sampled from it, so the two apps sit together on a home screen
     as a family. Note this is NOT the app's own dark_bg (#0B2818): the theme
     background is tuned for a low-light firing point, the icon for a home
     screen among other icons, and they are different problems. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp" android:height="108dp"
    android:viewportWidth="108" android:viewportHeight="108">
    <path android:fillColor="@color/icon_background" android:pathData="M0,0h108v108h-108z" />
</vector>
