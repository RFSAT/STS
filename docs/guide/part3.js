const B=require('./build.js');
const {P,H1,H2,H3,Bullet,Step,table,Spacer,callout,Paragraph,PageBreak}=B;
const body=[]; const push=(...x)=>x.forEach(e=>body.push(e));
push(
 new Paragraph({children:[new PageBreak()]}),
 H1("Target faces and rules"),
 H2("Target faces"),
 P("The Targets tab lists every face the app knows, with a preview and its full dimensions. The preview is drawn square so the face fills the width, with the scoring rings numbered as they are on the printed card — and the catalogue list stays visible underneath, which for the silhouette faces is the only way to tell an IPSC target from an IDPA one."),
 P("Sixteen faces ship with the app, including the ISSF 10 m air rifle and air pistol faces, the 25 m and 50 m pistol and rifle faces, the NRA and CMP high-power and smallbore faces, F-Class at 600 and 1000 yards, and the DSB/BDS faces."),
 P("Use this makes a face active. Copy & edit takes a catalogue face as a starting point for your own — this is how you add a club card or a face the app does not carry. Add my own target starts from nothing."),
 H2("Rule sets"),
 P("The Rules tab works the same way and shows every parameter of each match: shot count, scoring gauge, whether decimal scoring and inner tens apply, and the time limits where they matter. More than twenty rule sets ship with the app, from ISSF and NRA courses to NRL22, PRS, IPSC and IDPA stages."),
 callout("Check the numbers against your rulebook",[
  "The dimensions and parameters shipped with the app are nominal published figures, not certified extracts from a governing body's tables. They are editable for exactly that reason. Before a competition matters to you, check the face and the rule set against the rulebook in force."]),

 new Paragraph({children:[new PageBreak()]}),
 H1("Scoring with an AI service"),
 P("The app can send a photograph to an AI service and use what it reports. This is optional, off until you set it up, and it costs a fraction of a penny per card against your own account."),
 P("It exists because the app and a vision model are good at different things. The app MEASURES well — a hole it has found is placed to between 0.2 and 1.7 mm. It COUNTS badly: its usual failure is marking printing as shots. A vision model counts well and places badly: a position read off a picture carries several millimetres, which on a 10 m face can be a whole ring."),

 H2("Choosing a service"),
 P("Two are supported, and they are interchangeable. Both are sent the same flattened picture and the same question, and both are held to the same answer format, so a card scored by either arrives in the app in the same shape."),
 table(["Service","Where its key comes from"],
  [["Claude (Anthropic)","console.anthropic.com"],
   ["OpenAI","platform.openai.com"]],
  [2600,6760]),
 Spacer(140),
 P("Settings asks three separate questions about services, each saying what it governs. They are separate because they are separate decisions, and one shared setting could not express the useful combinations."),
 table(["Setting","What it decides"],
  [["On import, score with","What runs when you press Detect the hits and score them: Embedded, or a named service."],
   ["Second opinion asks","Which service the Second opinion button on Results calls."],
   ["Service to set up","Which service the key and model controls below are editing. It sends nothing on its own."]],
  [2600,6760]),
 Spacer(140),
 P("The first two are independent on purpose. Scoring with one service and checking with the other is the arrangement that makes a second opinion worth having; so is scoring with the app and checking with a service, which is the default."),
 P("Under Service to set up sits a model picker showing the models that service offers. The list is rebuilt when you change service, because an identifier from one means nothing to the other. Each list ends with Other, where you can type a model identifier by hand — a built-in list goes stale, and this way a newer model is never out of reach. Whatever you type must be able to read images and to answer against a schema."),
 P("Wherever the app is about to call a service, it names the one it is calling — “Asking OpenAI…”, “Scored by Claude (Anthropic)”. If a message names a service you did not choose, that is a bug worth reporting."),

 H2("Setting it up"),
 Step("Get an API key from the service's own console and put credit on the account.",3),
 Step("In Settings, under Service keys and models, set Service to set up to that service.",3),
 Step("Press Set API key and paste it in. The line above says which service it will apply to.",3),
 Step("Choose a model. The middle option of each list is the sensible default.",3),
 Step("Then choose where it is used: On import, score with, and Second opinion asks, under AI assistance.",3),
 Spacer(60),
 P("A key is kept for each service separately, so you can hold both and switch between them to compare. The screen lists every service and whether it has a key, so setting a second one is visibly not replacing the first."),
 callout("The key is not your chat password",[
  "It comes from the service's developer console and bills that account. The password you sign in to the chat product with will not work here — they are different systems.",
  "Keys are stored encrypted on the phone and are never written to the diagnostic log. If the device cannot provide encrypted storage a key is NOT saved in plain text as a fallback; you are told instead.",
  "Paste a key as a single line. A key copied from a wrapped display carries a line break, which cannot go in a network request — the app strips whitespace and tells you when it has, but a clean paste is better.",
  "Keys survive an ordinary app upgrade. They are deliberately left out of device backup: the key that unlocks them stays in the phone's own keystore and does not travel, so a restored copy could never be read. After moving to a new phone, enter them again.",
  "Every use needs a working connection, which most ranges do not have."]),

 H2("Choosing what scores a card"),
 P("Settings carries one line — On import, score with — and it decides what happens when you press Detect the hits and score them."),
 table(["Choice","What happens"],
  [["Embedded","The app's own detection runs, as it always has. A service is still available afterwards on the Results screen as a second opinion. This is the default and the more precise."],
   ["A named service","The app's detection does not run at all. That service finds and scores the shots. Registration is still the app's work, so the marks land exactly where you see them on your photograph. Falls back to Embedded if the service has no key — the picker still shows what you chose."]],
  [1700,7660]),

 H2("The second opinion"),
 P("Under Embedded, the Second opinion button on Results asks the service to look at the same card and reports where the two disagree — how many it sees, how many the app marked, and which marks are in dispute."),
 P("You are then offered the action that fits the disagreement:"),
 Bullet("When the app has marked MORE than the service sees, removal comes first. That is the common case, and the marks beyond the scoring rings are the likeliest to be wrong."),
 Bullet("When the service sees a shot the app missed, it is offered as a suggestion. The app then MEASURES that place before placing anything — the suggestion says where to look, and the measurement decides where the shot goes. If nothing hole-like is there, nothing is placed and you are told."),
 Spacer(60),
 P("Neither is applied without your agreement unless you have asked for it. Settings has an option to let the AI answer override the app outright, and the import setting above hands it the whole job."),
 P("The second opinion uses the service named under Second opinion asks, whichever service scores your imports. The summary it shows names the service that answered."),
 callout("What it cannot tell you",[
  "The service missing a real shot and the app inventing one look identical from the app's side. That is why removal is offered rather than performed, and why the dialog says so.",
  "Check the plot with My photo selected before agreeing to anything."]),

 H2("What to expect"),
 P("On the app's own reference card — seven real shots, hand-scored at nineteen points — the service counts seven and is right. The app, with the option to look outside the scoring rings switched on, marks sixteen: the seven real shots and nine pieces of printing. Nothing in the app's own measurements separates those nine from the two genuine misses among them; counting is the only thing that has ever arbitrated it."),
 P("That is the honest case for the feature, and its limit. It will tell you reliably that a card has been over-marked, and where. It will not place a shot as precisely as the app does when the app can see it."),

 new Paragraph({children:[new PageBreak()]}),
 H1("Detection options"),
 P("These live in Settings under Detection algorithms. Each carries one line saying what to expect, with More info for the reasoning behind it. Each can be switched independently so its effect can be judged on your own targets. The defaults are sensible; you do not have to touch any of them."),
 table(["Option","Default","What to expect"],
  [["Find shots in the photograph itself","On",
    "Leave on. Finds shots in the picture as it arrived, including inside the black aiming mark. With it off, centre hits are often missed entirely."],
   ["Estimate the background locally","On",
    "Leave on. Copes with a card that is lit unevenly, which is every card photographed on a table. With it off, one end of the card reads as holes everywhere and the other as nothing."],
   ["Require a shot to have the profile of a puncture","Off",
    "Fewer false hits: rejects printed marks that happen to look like shots."],
   ["Also find shots that missed the rings","Off",
    "Shows shots that missed. They score nothing — but printing near the edge of the card may be marked too, and the app cannot tell those apart. Leave off unless you want to see where a flyer went."],
   ["Scale from circles fitted to each printed ring","Off",
    "No visible difference on a flat card. May help on cards photographed at an angle."],
   ["Measure ring spacing only along the tilt axis","Off",
    "Experimental. Helped on some test images and hurt on others. Leave off unless you are testing it."]],
  [2500,900,5960])
);
module.exports=body;
