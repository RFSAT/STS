const B=require('./build.js');
const {P,H1,H2,H3,Bullet,Step,table,Spacer,callout,Paragraph,PageBreak}=B;
const body=[]; const push=(...x)=>x.forEach(e=>body.push(e));
push(
 new Paragraph({children:[new PageBreak()]}),
 H1("Scoring a target from a photograph"),
 P("This is the accurate route. Learn it first."),
 H2("Taking the photograph"),
 Bullet("Fill the frame with the card. The more of the picture the target occupies, the better every measurement is."),
 Bullet("Shoot square on. Small tilts are handled, but a steep angle is guesswork."),
 Bullet("Flat, even light. Avoid a hard shadow across the card and avoid direct flash, which blows out the paper and hides the holes."),
 Bullet("Do not brighten or adjust the photograph afterwards. The app reads colour as well as brightness, and an auto-levels pass throws away the difference it depends on."),
 Bullet("Photograph the clean card first, before the string, if you can — but see the note at the end of this section on what that is and is not good for."),
 H2("Loading and registering"),
 P("From the Session tab, choose Upload a target photo to score. The Import screen is laid out in four numbered steps that match what you have to do."),
 H3("Choose the photographs"),
 P("Pick the photo of the shot target. Optionally pick a photo of the clean target as well."),
 H3("Course of fire"),
 P("Confirm the Rules, the Target face and the Distance. These carry over from Home, so normally you only check them."),
 H3("Register the target"),
 P("Registration is the app working out where the target is in the picture and how big it is. Three buttons:"),
 table(["Button","When to use it"],
  [["Identify target and register","Try this first, and it runs automatically when the picture loads. It fits the printed rings, identifies which catalogue face they belong to, adopts that face, and registers in one action."],
   ["Auto-detect the target","The fallback. Finds the black aiming mark only. Less accurate, because one circle carries less information than the whole ring family."],
   ["Register","Manual. Drag the box onto the outer scoring ring yourself. Use it when the other two fail — a badly cropped or very low-contrast card."]],
  [2800,6560]),
 Spacer(140),
 P("If the card is at an angle, the Tilt and rotation controls let you correct it. Apply estimated tilt fills them in from what the app measured; Reset tilt clears them. Leave these alone unless the registration is visibly wrong."),
 Spacer(60),
 P("Registration is always the app's own work, even when an AI service is scoring the card. Without knowing where the card is and how big it is there is no millimetre grid, and nothing could be drawn in the right place."),
 H3("Score"),
 P("Press Detect the hits and score them. What runs here depends on the Settings choice “On import, score with” — the app's own detection, or the AI service you have chosen. Choose whether to replace the shots already in the session or add to them. The shot distribution appears below, and Open the full results takes you to the Results tab."),
 H2("Check the result before you trust it"),
 P("On the Results screen, switch the plot to My photo. This lays the detected shots over your own photograph, and it is the only view in which a missed hole is obvious. On template a missing shot just looks like a shot you did not fire."),
 P("The photograph is kept with the session, so closing the app and coming back to it later leaves both the score and the picture where you left them. It is discarded only when you start a new session."),
 P("Add, move or delete shots as needed. Moving a shot keeps the app's sub-millimetre estimate of its centre where that estimate was good; deleting and re-tapping throws it away. Nudge rather than replace."),
 H2("About the clean photograph"),
 P("With two photographs the app can compare them and look for what changed, which in principle cancels everything printed and leaves only holes."),
 callout("Measured, and it does not work hand-held",[
  "Two photographs of the same UNPUNCHED card, taken by hand a moment apart, differ by three to ten phantom holes. Registering each frame separately makes it worse, not better — so it is not camera shake.",
  "A printed ring line is about half a pixel wide in the flattened image, and no alignment good enough to make those lines cancel is available from a ring fit. Add a lighting gradient that shifts as the card is handled and the residue swamps a single new hole.",
  "It has not been tested from a FIXED MOUNT with the lighting undisturbed, which is what it was designed for. Until it has been, treat the clean photograph as unproven rather than as the accurate route."]),
 new Paragraph({children:[new PageBreak()]}),
 H1("Scoring live from the camera"),
 P("Live scoring watches the target and adds each shot as it appears. It is convenient and it is less forgiving than a photograph — it depends on a steady mount, steady light and an unobstructed view."),
 H2("Setting up"),
 Step("Mount the phone on a stand with a clear view of the target. It must not move for the rest of the string.",2),
 Step("On the Session tab, choose the frame source: the phone camera, or a network stream such as an IP camera (an http://…/video or rtsp://… address).",2),
 Step("Set the Analysis resolution. 1920 × 1080 is the default and the right choice for most cards. Higher costs speed; lower risks losing small holes.",2),
 Step("Press Lock exposure and focus. If the camera re-focuses or re-meters mid-string, the picture changes underneath the detector and shots can be missed or invented.",2),
 Step("Use the alignment guide to line the phone up — see below.",2),
 Step("Press Capture clean target (reference), then Start live detection.",2),
 H2("The alignment guide"),
 P("The guide draws the rings of the selected face over the camera preview, at their true proportions. Line the printed rings up with the drawn ones."),
 table(["Option","What it draws"],
  [["Nothing","No overlay."],["Simple crosshair","A cross at the centre of the frame."],
   ["Rings of the selected target","The rings of the face you have chosen, at true proportions."],
   ["Rings and crosshair","Both."]],[3000,6360]),
 Spacer(140),
 P("Guide size sets how much of the screen the rings occupy. A word in the corner tells you continuously whether the card in front of the camera matches the face you have selected:"),
 table(["Message","Meaning"],
  [["Aim at the target","The rings cannot be fitted — cropped, too dark, or too steep an angle."],
   ["Checking…","A measurement is in progress."],
   ["Match OK","The card matches the selected face. The guide is drawn solid."],
   ["Wrong target face","The card does not match. The guide is drawn finely dashed, and the app will normally switch to the face it recognises and tell you it has."]],
  [2600,6760]),
 Spacer(140),
 P("The state is shown three ways at once — by colour, by whether the line is solid or dashed, and by the word — so it still reads correctly under the night themes, where the colour is deliberately left alone, and for a colour-blind shooter in daylight."),
 callout("Do not force a match",[
  "The rings are drawn at the selected face's own proportions, so moving closer or further away resizes them all together. A different face can never be made to line up by changing distance. If the guide says the face is wrong, it is wrong."]),
 H2("During and after the string"),
 P("A hole must appear in three consecutive frames before it is recorded. A flicker, a passing shadow or a moment of camera shake will not add a shot."),
 P("Score the target now (single frame) scores whatever is in view immediately, without waiting. Undo shot removes the last one recorded. When the string is over, go to Results and check the plot against the card."),
 new Paragraph({children:[new PageBreak()]}),
 H1("Reading the Results screen"),
 H2("The plot"),
 P("Shots are drawn as hollow circles with a small cross marking the centre of each, so that a shot on a ring line does not hide the line. Pinch to zoom and drag to pan; Reset view returns to the default framing. Zoom and pan survive editing, so you can nudge a shot without losing your place."),
 table(["Control","What it does"],
  [["My photo / Template","Switches between your own rectified photograph and the printed face. One or the other, never both. The photograph is kept with the session and is still there after the app has been closed and reopened."],
   ["Whole card / Scoring area","Whether the view fills with the scoring rings or the entire card."],
   ["Add","Tap the plot to place a shot the detector missed."],
   ["Move","Drag a shot to correct its position."],
   ["Delete","Remove a shot that was never fired."],
   ["Second opinion","Ask the AI service to check the card. Only when a key is set — see the next chapter."],
   ["Reset view","Return zoom and pan to the default."]],
  [2600,6760]),
 H2("Score, group and correction"),
 Bullet("The headline is the total, and the maximum for the rule set where one applies. Inner tens are counted where the rules call for them."),
 Bullet("Group gives the centre of the group and its dispersion — how far your shots are from each other rather than from the middle."),
 Bullet("Sight correction converts the group offset into clicks for the sight in your active profile set, into a rear-sight movement in millimetres for an unclicked iron sight, or into a hold-off where there is no sight at all. When the group is already closer to the point of aim than one click would move it, the box says so and the line beneath it gives the residual — the two never disagree."),
 Bullet("Under that, the same correction as an angle: elevation and windage, in MRAD and MOA, in a column so the figures can be compared at a glance."),
 Bullet("Shot distribution shows how the shots fall across the rings."),
 H2("Recording and sharing"),
 P("Stage time is for practical disciplines where the score depends on it. Notes is free text — conditions, hold-off, anything worth remembering when you look back at this string in a month."),
 P("Share report produces a readable summary. Export CSV gives you the shot coordinates and scores for your own analysis. Finish and start a new session closes the current string and clears the plot for the next one."),
 Spacer(60),
 P("Shots the app measured itself and shots placed by hand — or by an AI service — are recorded differently, and a shared report says which is which. A position that was not measured is never presented as though it had been.")
);
module.exports=body;
